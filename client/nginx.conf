user nginx;
worker_processes 1;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                  '$status $body_bytes_sent "$http_referer" '
                  '"$http_user_agent" "$http_x_forwarded_for"';
  access_log /var/log/nginx/access.log main;
  sendfile on;
  keepalive_timeout 65;

  # Редирект HTTP → HTTPS
  server {
    listen 80;
    server_name 176.108.249.113.nip.io;
    return 301 https://$host$request_uri;
  }

  # Основной HTTPS-сервер
  server {
    listen 443 ssl;
    server_name 176.108.249.113.nip.io;

    ssl_certificate /certs/176.108.249.113.nip.io.crt;
    ssl_certificate_key /certs/176.108.249.113.nip.io.key;

    # Отдача статики фронтенда
    location / {
      root /client;
      try_files $uri $uri/ /index.html;
    }

    # Проксирование API к бэкенду
    location /api/ {
      proxy_pass http://backend:8000/;  # важно: слэш в конце!
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
}
